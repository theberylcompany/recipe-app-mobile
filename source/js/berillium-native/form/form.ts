import {  StyleSheet } from 'react-native'

const login = StyleSheet.create({
    field: {
        height: 50,
        width: 280,
        borderRadius: 10,
        backgroundColor: '#EFEFEF'
    }
}) 

export {login}