import {  StyleSheet } from 'react-native'

const heading = StyleSheet.create({
    h2: {
        fontSize: 24,
    },
    h3: {
        fontSize: 20
    }
})

const para = {
    text: {
        fontSize: 16
    }
}

export {heading, para}